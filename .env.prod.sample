# === Server ===
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# === Mongo ===
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster-host>/
MONGO_DATABASE=soboro
MONGO_USERNAME=<user>

# === JWT ===
JWT_SECRET=<replace-in-EC2>

# === 3rd-party Keys ===
OPENAI_API_KEY=<replace-in-EC2>
GEMINI_API_KEY=<replace-in-EC2>
KAKAO_REST_API_KEY=<replace-in-EC2>

# === Google NLP ===
GOOGLE_NLP_KEY_PATH=/run/secrets/google-nlp.json

# === CORS ===
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://3.34.123.45:3000,https://sovolo.dev

# .env 파일의 샘플 구조입니다.
